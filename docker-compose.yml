version: '3'

services:
#   backend:
#     build: ./app/
#     command: "uvicorn main_docker:app --host 0.0.0.0 --port 8000 --reload"
#     ports:
#       - 8000:8000
# #    volumes:
# #      - ./src:/app

  mysql:
    image: mysql:8.0
    restart: always
#    command:
#      - '--secure-file-priv=""'
#      - '--local-infile=1'
    environment:
      MYSQL_DATABASE: 'bank'
      MYSQL_ROOT_PASSWORD: 'password'
      MYSQL_ROOT_USER: 'root'
    ports:
      - '3306:3306'
#    volumes:
#          - dbdata:/var/lib/mysql
# 
  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    restart: always
    environment:
      PMA_HOST: mysql
      PMA_PORT: 3306
      MYSQL_ROOT_PASSWORD: 'password'
    ports:
      - '8080:80'
    depends_on:
      - mysql

volumes:
    dbdata: